// Файл для работы с моими кодами, которые могут быть нужны для решения задач, но не нужны непосредственно в задании

// странный код из 50 задачи, будет нужен для решения куска с приемом значений массива при помощи метода

void ElementValues(int number) //, int m)
{
    for (int n = 0; n < 2; n++)
    {
        Console.WriteLine("Введите позицию элемента в двумерном массиве: ");
        int numb = Convert.ToInt32(Console.ReadLine());
    }
}